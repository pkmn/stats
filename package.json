{
  "name": "pokemon-showdown-stats",
  "private": true,
  "version": "0.0.1",
  "description": "The stats processing logic for the Pokémon Showdown battle simulator",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pkmn-cc/Pokemon-Showdown-Stats.git"
  },
  "author": "Kirk Scheibelhut",
  "license": "MIT",
  "dependencies": {
    "stats": "file:stats",
    "@types/node": "^11.13.4",
    "ps": "file:ps"
  },
  "devDependencies": {
    "gts": "^0.9.0",
    "source-map-support": "^0.5.12",
    "subpackage": "^1.1.0",
    "typescript": "^3.4.3"
  },
  "scripts": {
    "check": "gts check && subpkg run check",
    "clean": "gts clean && subpkg run check",
    "compile": "tsc -p . && subpkg run compile",
    "fix": "gts fix && subpkg run fix",
    "test": "subpkg run test",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "postinstall": "subpkg install"
  },
  "subPackages": [
    "stats"
  ]
}
